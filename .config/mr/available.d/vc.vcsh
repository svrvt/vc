[$HOME/.config/vcsh/repo.d/vc.git]
order = 1
checkout = vcsh clone git@github.com:chikh-chikh/vc.git
init =
    git fetch origin
    git checkout main -f
# update = 
#     git push origin

# vim: ft=ini syntax=sh

